// Étape 1 du pipeline : génère une question structurée à partir du thème, niveau, chunks, historique
// cf. section 4 du plan pour le schéma JSON de sortie (question, topic, skill, difficulty, expected_concepts, source_chunk_ids)
export async function generateQuestion(ai: Ai, /* params */) {
  throw new Error("TODO");
}

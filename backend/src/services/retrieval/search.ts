// Recherche vectorielle top-K (question + réponse candidat + thème + difficulté)
// cf. section 3.D du plan
export async function searchChunks(vectorize: VectorizeIndex, query: string, topK = 10) {
  throw new Error("TODO");
}

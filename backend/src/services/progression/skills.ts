// Applique les skill_updates renvoyés par evaluateAnswer, met à jour SkillProgress en D1,
// et détermine quelles compétences faibles doivent réapparaître plus souvent
export async function applySkillUpdates(db: D1Database, userId: string, updates: unknown[]) {
  throw new Error("TODO");
}

export async function pickNextSkillFocus(db: D1Database, userId: string) {
  throw new Error("TODO");
}

// Page de configuration : choix du corpus, thèmes, mode d'entretien (pedagogique/recruteur/senior), niveau, nb questions
// POST /api/interviews -> redirige vers /interviews/:id
export default function Configuration() {
  return null;
}

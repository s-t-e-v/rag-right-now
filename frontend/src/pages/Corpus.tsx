// Page Corpus : upload de fichiers, état de l'indexation, liste des documents, suppression
// Utilise <UploadZone /> + GET/POST /api/corpora, POST /api/corpora/:id/documents
export default function Corpus() {
  return null;
}

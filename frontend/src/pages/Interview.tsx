// Page d'entretien : question courante, zone de réponse, feedback + citations, enchaînement des tours
// POST /api/interviews/:id/answer -> { evaluation, next_question, skill_updates }
// cf. useInterviewSession()
export default function Interview() {
  return null;
}

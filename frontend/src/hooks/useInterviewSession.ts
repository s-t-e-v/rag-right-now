// Gère l'état d'une InterviewSession côté client : question courante, historique des tours,
// envoi de réponse (useMutation vers POST /api/interviews/:id/answer)
export function useInterviewSession(sessionId: string) {
  // TODO
}
